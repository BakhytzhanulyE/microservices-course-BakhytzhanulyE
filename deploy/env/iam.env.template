# ----------------------------
# gRPC-сервер
# ----------------------------
GRPC_HOST=${IAM_GRPC_HOST}
GRPC_PORT=${IAM_GRPC_PORT}

# ----------------------------
# Логгер
# ----------------------------
LOGGER_LEVEL=${COMMON_LOGGER_LEVEL}
LOGGER_AS_JSON=${COMMON_LOGGER_AS_JSON}

# ----------------------------
# PostgreSQL
# ----------------------------
POSTGRES_IMAGE_NAME=${IAM_POSTGRES_IMAGE_NAME}
EXTERNAL_POSTGRES_PORT=${IAM_POSTGRES_EXTERNAL_PORT}
POSTGRES_HOST=${IAM_POSTGRES_HOST}
POSTGRES_PORT=${IAM_POSTGRES_PORT}
POSTGRES_DB=${IAM_POSTGRES_DB}
POSTGRES_USER=${IAM_POSTGRES_USER}
POSTGRES_PASSWORD=${IAM_POSTGRES_PASSWORD}

# ----------------------------
# Redis
# ----------------------------
REDIS_IMAGE_NAME=${IAM_REDIS_IMAGE_NAME}
EXTERNAL_REDIS_PORT=${IAM_REDIS_EXTERNAL_PORT}
REDIS_HOST=${IAM_REDIS_HOST}
REDIS_PORT=${IAM_REDIS_PORT}
REDIS_DB=${IAM_REDIS_DB}

# ----------------------------
# JWT
# ----------------------------
JWT_SECRET_KEY=${IAM_JWT_SECRET_KEY}
JWT_ACCESS_TTL=${IAM_JWT_ACCESS_TTL}
JWT_REFRESH_TTL=${IAM_JWT_REFRESH_TTL}

# ----------------------------
# Наблюдаемость
# ----------------------------
METRICS_ENABLED=${IAM_METRICS_ENABLED}
METRICS_PORT=${IAM_METRICS_PORT}
OTEL_SERVICE_NAME=iam
OTEL_EXPORTER_OTLP_ENDPOINT=${COMMON_OTLP_ENDPOINT}
